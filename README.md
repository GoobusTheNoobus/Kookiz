# Kookiz

Kookiz (pronounced "Cookies") is a very delicious programming language (WIP). It is interpreted, and attempts to introduce low level concepts like pointers into the world of interpreted languages. It also supports high level concepts, like classes, higher order functions, and dynamic typing.